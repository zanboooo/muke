# 公开库护栏的豁免清单。格式：
#     <路径>  <规则id>[,<规则id>]   # 为什么这不是泄露
#
# 规矩：
#  · 只豁免「这个文件的这一条规则」，不给整个文件开天窗
#  · 理由必填（# 后面），没写理由脚本直接报错
#  · 加豁免之前先问一句：是真的误杀，还是我在给自己开后门？
#    绕过的代价是再删一次库 —— 2026-09-06 已经删过一次。

tools/guards.js       salary-word2                 # 命中的是规则自身的正则源码，不是数据
tools/guard-tree.js   ump,emp-no,ktp,phone-id,salary-word,salary-word2,money-rp,lark-id,gh-token  # 同上：这个文件就是规则表，必然命中自己
